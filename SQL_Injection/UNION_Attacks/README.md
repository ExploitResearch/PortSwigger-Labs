# UNION Attacks

SQL injection UNION attacks exploit the UNION SQL operator to combine the results of the original query with attacker-controlled results. These labs cover identifying the correct number of columns, finding columns with compatible data types, and extracting data from arbitrary database tables.

## Labs

- [SQL injection UNION attack, finding a column containing text](./SQL_injection_UNION_attack,_finding_a_column_containing_text.md)
