# Filter Bypass

SQL injection with various filter bypass techniques. These labs demonstrate how applications attempt to block SQL injection using input filters and how attackers can circumvent them using encoding, alternative syntax, and creative payload construction.

## Labs

- [SQL injection with filter bypass via XML encoding](./SQL_injection_with_filter_bypass_via_XML_encoding.md)
